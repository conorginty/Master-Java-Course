package Section_8_Arrays_JavaInbuiltLists_AutoboxingAndUnboxing.Arrays_Parts;

public class ArraysRecap {
    // 101. Arrays Recap

    // An array = data structure that allows you to store multiple values of the same type into a single variable
    // The default values of numeric array elements are set to zero (0).
    // Arrays are Zero-Indexed: An array with n elements is indexed from 0 to n-1
    // If we try to access an index that is out of range, Java will give us an ArrayIndexOutOfBoundsException, which
    // indicates that the index is out of range (i.e. out of bounds)
    // To access array elements, we use square brackets (i.e. [])
}
