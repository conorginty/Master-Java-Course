package Section_11_NamingConventions_Packages_Static_Final_KWs.FinalStatement;

public class FinalStatement {
    // 151. The final statement

    // final statement/keyword is generally used to define constant values
    // Strictly speaking final fields are NOT constants, as they can be modified, but only once, and any modification
    // must be performed before the class constructor finishes. This means we can assign a final field its value
    // when we first declare it or in the constructor.
}
